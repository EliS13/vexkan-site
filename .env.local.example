# Sign-in is optional. Without these the site still works and saves to the
# browser only. With them, notebooks and season plans follow a team between
# devices.
#
# 1. Make a free project at https://supabase.com
# 2. Run supabase/migrations/0001_init.sql in the SQL editor
# 3. Project Settings, API, copy the two values below
# 4. Save this file as .env.local and restart the dev server
#
# The anon key is safe to expose. Row level security in the database is what
# keeps one team from reading another team's rows.

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Live Alberta competitions from events.vex.com.
#
# Server-only on purpose: no NEXT_PUBLIC_ prefix, because this is a credential
# and that prefix would ship it to the browser in the page source.
#
# Without it the competitions section falls back to the events kept by hand in
# src/content/club/events.ts and says the live list is not switched on.
VEX_API_TOKEN=

# Optional. Points the events fetch at a different base URL, for a fixture or a
# staging API. Leave unset to use https://events.vex.com/api/v2.
VEX_API_BASE=

# Coach passcode for the sign-in kiosk at signin.vexkan.ca.
#
# Gates signing new members up and overriding a failed face match. Not a real
# auth system, just enough that a student cannot wander into enrollment.
#
# Server-only on purpose: no NEXT_PUBLIC_ prefix. The browser sends a candidate
# and is told yes or no; the passcode itself never reaches the page.
KIOSK_ADMIN_PASSCODE=

# The kiosk's database. Without these, a deployed kiosk has nowhere durable to
# save: /tmp on Vercel is per-instance and cleared on cold starts, so members
# vanish and the roster reads "0 of 0" minutes after someone signs up.
#
# The service role key bypasses row level security, which is what lets the
# kiosk's server routes read and write while the public anon key can touch
# nothing. Server-only: no NEXT_PUBLIC_ prefix, ever. Treat it like a password.
#
# 1. Run supabase/migrations/0003_kiosk.sql in the SQL editor
# 2. Project Settings, API, copy the Project URL and the service_role key
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Restricts sign-in to the club's own network, so a member cannot mark
# themselves present from home. Comma-separated public IPs or CIDR blocks.
# Empty allows anywhere, deliberately: a lapsed address should not lock the club
# out on a meeting night.
KIOSK_ALLOWED_IPS=
